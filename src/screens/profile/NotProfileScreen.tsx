import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const NotProfileScreen = (props: Props) => {
  return (
    <View>
      <Text>NotProfileScreen</Text>
    </View>
  )
}

export default NotProfileScreen

const styles = StyleSheet.create({})